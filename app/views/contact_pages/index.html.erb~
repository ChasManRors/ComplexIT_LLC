<h1>Listing contact_pages</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>

<% @contact_pages.each do |contact_page| %>
  <tr>
    <td><%=h contact_page.title %></td>
    <td><%=h contact_page.body %></td>
    <td><%= link_to 'Show', contact_page %></td>
    <td><%= link_to 'Edit', edit_contact_page_path(contact_page) %></td>
    <td><%= link_to 'Destroy', contact_page, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New contact_page', new_contact_page_path %>