<h1>Listing services_pages</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
  </tr>

<% @services_pages.each do |services_page| %>
  <tr>
    <td><%=h services_page.title %></td>
    <td><%=h services_page.body %></td>
    <td><%= link_to 'Show', services_page %></td>
    <td><%= link_to 'Edit', edit_services_page_path(services_page) %></td>
    <td><%= link_to 'Destroy', services_page, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New services_page', new_services_page_path %>